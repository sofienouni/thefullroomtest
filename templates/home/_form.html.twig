
{{ form_start(form) }}
    <div class="row g-3">
        <div class="form-outline mb-4">
            <div class="form-group">
                <label>Date Début</label>
                {{ form_widget(form.start_date,{ 'attr': {'style': 'float: right;', 'class' : 'form-control'} }) }}
            </div>
            <div class="form-group">
                <label>Date Fin</label>
                {{ form_widget(form.end_date,{ 'attr': {'style': 'float: right;', 'class' : 'form-control'} }) }}
            </div>
            <div class="form-group">
                <label>Commentaire</label>
                {{ form_widget(form.comment,{ 'attr': {'style': 'float: right;', 'class' : 'form-control'} }) }}
            </div>
            <div class="form-group">
                <label>Status</label>
                {{ form_widget(form.status,{ 'attr': {'style': 'float: right;', 'class' : 'form-control'} }) }}
            </div>

        </div>

    </div>
<button class="w-100 btn btn-primary btn-lg" type="submit">{{ button_label|default('Save') }}</button>



{{ form_end(form) }}
